# `RolloutConfigMismatch`

An append's config fingerprint disagrees with the set's manifest — refused rather than silently mixed.

```python
RolloutConfigMismatch()
```

Defined in [`interlens.arena.rollouts`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/rollouts.py#L149-L150)

**Inherits from:** `RuntimeError`
