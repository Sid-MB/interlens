# `ReplayError`

A stored turn could not be matched to the state machine's pending request.

```python
ReplayError()
```

Defined in [`interlens.arena.replay`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/replay.py#L41-L42)

**Inherits from:** `RuntimeError`
