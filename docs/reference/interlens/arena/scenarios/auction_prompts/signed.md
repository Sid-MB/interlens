# `signed`

A loading or attribute entry as the reviewed prose prints it: `+1`, `0`, or `{MINUS}1` with a Unicode minus.

```python
signed(x) -> str
```

Defined in [`interlens.arena.scenarios.auction_prompts`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/scenarios/auction_prompts.py#L172-L180)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `x` |  | *required* |  |
