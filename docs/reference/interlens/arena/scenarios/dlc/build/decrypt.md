# `decrypt`

Decrypt one BrowseComp-Plus field with the dataset's published canary.

```python
decrypt(ciphertext_b64: str) -> str
```

Defined in [`interlens.arena.scenarios.dlc.build`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/scenarios/dlc/build.py#L337-L340)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `ciphertext_b64` | `str` | *required* |  |
