# `char_balanced_split`

Contiguous split of whole lines into `k` blocks with roughly equal characters.

```python
char_balanced_split(lines: list[str], k: int = 4) -> list[list[str]]
```

Defined in [`interlens.arena.scenarios.dlc.build`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/scenarios/dlc/build.py#L102-L115)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `lines` | `list[str]` | *required* |  |
| `k` | `int` | `4` |  |
