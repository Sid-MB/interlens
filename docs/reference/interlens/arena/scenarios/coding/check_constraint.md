# `check_constraint`

checker(code_str) -> bool via ast/static analysis; SyntaxError -> False.

```python
check_constraint(cid: str, code: str) -> bool
```

Defined in [`interlens.arena.scenarios.coding`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/scenarios/coding.py#L495-L501)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `cid` | `str` | *required* |  |
| `code` | `str` | *required* |  |
