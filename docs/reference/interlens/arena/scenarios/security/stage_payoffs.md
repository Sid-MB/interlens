# `stage_payoffs`

Stage payoffs `(pa, pb)` for true actions `a` (seat 0) and `b` (seat 1).

```python
stage_payoffs(a: str, b: str, b_fs: int) -> tuple[int, int]
```

Defined in [`interlens.arena.scenarios.security`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/scenarios/security.py#L60-L73)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `a` | `str` | *required* |  |
| `b` | `str` | *required* |  |
| `b_fs` | `int` | *required* |  |
