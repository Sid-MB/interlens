# `taxonomy_rows`

The taxonomy as a static table (id, name, tier, citations, implemented) for docs/report headers.

```python
taxonomy_rows() -> list[dict]
```

Defined in [`interlens.arena.negotiation.analysis.taxonomy`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/negotiation/analysis/taxonomy.py#L326-L330)
