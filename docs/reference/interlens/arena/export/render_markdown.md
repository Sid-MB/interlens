# `render_markdown`

A full markdown transcript for one episode (setup header + per-turn + outcome/welfare).

```python
render_markdown(episode: dict, instance: dict | None = None) -> str
```

Defined in [`interlens.arena.export`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/export.py#L123-L189)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `episode` | `dict` | *required* |  |
| `instance` | `dict \| None` | `None` |  |
