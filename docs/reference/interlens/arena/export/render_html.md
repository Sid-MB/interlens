# `render_html`

A self-contained (inline-CSS, no external assets) HTML transcript for one episode.

```python
render_html(episode: dict, instance: dict | None = None) -> str
```

Defined in [`interlens.arena.export`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/export.py#L208-L255)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `episode` | `dict` | *required* |  |
| `instance` | `dict \| None` | `None` |  |
