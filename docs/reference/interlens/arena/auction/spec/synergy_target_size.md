# `synergy_target_size`

Size of the private synergy target SET at `n_items` lots: a pair below :data:`SYNERGY_TARGET_LARGE_THRESHOLD` lots, a triple at or above (design.md §2.2).

```python
synergy_target_size(n_items: int) -> int
```

Defined in [`interlens.arena.auction.spec`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/auction/spec.py#L658-L661)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `n_items` | `int` | *required* |  |
