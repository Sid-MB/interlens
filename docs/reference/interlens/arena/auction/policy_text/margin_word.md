# `margin_word`

`"clearly"` when the gap exceeds 10% of the best-response value, `"marginally"` otherwise -- a qualitative label over two numbers that are never themselves emitted.

```python
margin_word(decision) -> str
```

Defined in [`interlens.arena.auction.policy_text`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/auction/policy_text.py#L173-L180)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `decision` |  | *required* |  |
