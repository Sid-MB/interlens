# `scenario_scorer`

Score from the scenario's exact outcome (stored by the solver): `success` (bool) and `primary` (the scenario's normalized primary metric), with the full outcome dict in the score metadata.

```python
scenario_scorer()
```

Defined in [`interlens.arena.inspect.adapter`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/inspect/adapter.py#L273-L289)
