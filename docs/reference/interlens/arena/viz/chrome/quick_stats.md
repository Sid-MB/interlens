# `quick_stats`

The two-or-three-number read that rides in the top bar and stays visible while scrolling.

```python
quick_stats(payload: dict) -> str
```

Defined in [`interlens.arena.viz.chrome`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/viz/chrome.py#L251-L260)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `payload` | `dict` | *required* |  |
