# `inject_nav`

Put a page's navigation into its top bar.

```python
inject_nav(page_html: str, nav: str) -> str
```

Defined in [`interlens.arena.viz.chrome`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/viz/chrome.py#L178-L180)

A no-op on a page that carries no marker.

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `page_html` | `str` | *required* |  |
| `nav` | `str` | *required* |  |
