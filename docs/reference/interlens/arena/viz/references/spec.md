# `spec`

The :data:`REFERENCES` entry for a (possibly aliased) name, or `None` for a generic scored oracle.

```python
spec(name: str) -> dict | None
```

Defined in [`interlens.arena.viz.references`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/viz/references.py#L170-L172)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `name` | `str` | *required* |  |
