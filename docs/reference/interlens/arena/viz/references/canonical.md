# `canonical`

The canonical reference name for a stored one, or the stored name unchanged when it is not a reference.

```python
canonical(name: str) -> str
```

Defined in [`interlens.arena.viz.references`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/viz/references.py#L165-L167)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `name` | `str` | *required* |  |
