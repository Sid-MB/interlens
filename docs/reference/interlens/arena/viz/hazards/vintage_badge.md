# `vintage_badge`

The sticky quick-read marker for a spoiled vintage, so the hazard survives scrolling past the banner.

```python
vintage_badge(vintage: dict | None) -> str
```

Defined in [`interlens.arena.viz.hazards`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/arena/viz/hazards.py#L204-L209)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `vintage` | `dict \| None` | *required* |  |
