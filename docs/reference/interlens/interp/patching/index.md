# `patching`

Module `interlens.interp.patching`

## Classes

| Name | Summary |
|---|---|
| [`Patch`](Patch.md) | Activation patching: overwrite a decoder layer's residual at specific token `positions` with saved `activations` (captured from another run/branch). |
