# `moe_topk`

Experts selected per token (`config.num_experts_per_tok`).

```python
moe_topk(model: 'PreTrainedModel') -> int
```

Defined in [`interlens.interp.routing`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/interp/routing.py#L48-L50)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `model` | `'PreTrainedModel'` | *required* |  |
