# `str_to_dtype`

`'bfloat16'` -> `torch.bfloat16` (accepts a `torch.dtype` unchanged).

```python
str_to_dtype(name) -> 'torch.dtype'
```

Defined in [`interlens.participant.participants.model_participant`](index.md) · [source](https://github.com/Sid-MB/interlens/blob/main/src/interlens/participant/participants/model_participant.py#L46-L48)

**Parameters**

| Name | Type | Default | Description |
|---|---|---|---|
| `name` |  | *required* |  |
