# `tool_call`

Module `interlens.tools.tool_call`

## Classes

| Name | Summary |
|---|---|
| [`ToolCall`](ToolCall.md) | A parsed request from the model to invoke a tool: the tool `name` and its `arguments`. |
| [`ToolResult`](ToolResult.md) | The outcome of executing a `ToolCall`: the tool `name` and its string `output` (or error text). |
