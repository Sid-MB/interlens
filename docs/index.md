<!-- Landing page: include the top-level README verbatim so the intro has a single source of truth.
     (Relative links in the README that point into docs/examples/ resolve correctly once served, since the
     examples live under this docs tree.) -->
--8<-- "README.md"
